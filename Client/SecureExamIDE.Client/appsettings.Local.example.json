{
  // Copy this file to appsettings.Local.json (git-ignored) and point it at the API.
  // Running the API on this machine: http://localhost:5000
  // Running it on another machine on the network: http://<that machine's IP address>:5000
  "Api": {
    "BaseUrl": "http://localhost:5000"
  }
}
